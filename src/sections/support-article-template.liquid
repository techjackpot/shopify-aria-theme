<div id="section-{{ section.id }}" class="Section Section-Blog-Posts" data-section-id="{{ section.id }}" data-section-type="article-list">
  <div class="Blog">
    <div class="Container Container--narrow">
      <header class="PageHeader">
        <div class="SectionHeader SectionHeader--center">
          <h1 class="SectionHeader__Heading Heading u-h1" data-aos="fade-right">
            <a href="/pages/support-archive">Support center</a> / {{ blog.title }}
          </h1>
          <p class="ArticleHeader" data-aos="fade-right">{{ article.title }}</p>
        </div>
      </header>
      <div class="ArticleContainer">
        <div class="ArticleListWrapper">
          <div class="ArticleList">
            {%- assign articles = blog.articles | sort: 'published_at' -%}
            {%- for item in articles -%}
              <div class="ArticleItem">
                <a href="{{ item.url }}" class="{%- if item.handle == article.handle -%}active{%- endif -%}">{{ item.title }}</a>
              </div>
            {%- endfor -%}
          </div>
        </div>
        <div class="ArticleContent Rte" data-aos="fade-up">
          {{ article.content }}
        </div>
      </div>
    </div>
  </div>
</div>

{% schema %}
{
  "name": "Support - Article page"
}
{% endschema %}