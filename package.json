{
  "name": "shopify-aria-theme",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "dependencies": {
    "@shopify/themekit": "^1.1.6"
  },
  "devDependencies": {
    "dotenv": "^8.2.0",
    "gulp": "^4.0.2",
    "gulp-autoprefixer": "^7.0.1",
    "gulp-rename": "^2.0.0",
    "gulp-replace": "^1.0.0",
    "gulp-sass": "^4.0.2",
    "gulp-sourcemaps": "^2.6.5",
    "nodemon": "^2.0.2"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "download": "node -r dotenv/config scripts/download",
    "download:prod": "SHOPIFY_ENV=production node -r dotenv/config scripts/download",
    "start": "node -r dotenv/config scripts/watch",
    "start:prod": "SHOPIFY_ENV=production node -r dotenv/config scripts/watch",
    "deploy": "node -r dotenv/config scripts/deploy",
    "deploy:prod": "SHOPIFY_ENV=production node -r dotenv/config scripts/deploy",
    "deploy-settings": "node -r dotenv/config scripts/deploy-settings",
    "deploy-settings:prod": "SHOPIFY_ENV=production node -r dotenv/config scripts/deploy-settings",
    "gulp": "gulp"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/techjackpot/shopify-aria-theme.git"
  },
  "author": "techjackpot",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/techjackpot/shopify-aria-theme/issues"
  },
  "homepage": "https://github.com/techjackpot/shopify-aria-theme#readme"
}
